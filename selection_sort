def Selection_sort(arr):
  for start in range(0,len(arr)):
    minpos = start
    for i in range(start, len(arr)):
      if arr[i] < arr[minpos]:
        minpos = i
    (arr[start], arr[minpos]) = (arr[minpos],arr[start])


arr = [1,7,4,5,8,0,3,2,1]
Selection_sort(arr)
print(arr)
